# IHT - Dotfiles

## Install

```bash
curl -fsSL https://raw.githubusercontent.com/JuicyGoose007-coder/IHT/main/scripts/install.sh | bash
```

This will clone the repo to `~/IHT` and copy all configs into place.
